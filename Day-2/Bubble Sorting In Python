#Bubble Sorting Technique --> Compare each element with the entire list

arr=list(map(int,input().split()))
n=len(arr)

for i in range(n):
    for j in range(n):
        if(arr[j] > arr[i]):
            arr[j] , arr[i] = arr[i] , arr[j]
            
print(arr)
